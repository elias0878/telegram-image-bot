# بوت تيليجرام للصور العشوائية 📸

بوت تيليجرام بسيط يعرض صوراً عشوائية من قاعدة البيانات عند الضغط على زر.

## المميزات

- 🎲 عرض صور عشوائية من قاعدة البيانات
- 📊 تتبع عدد الصور
- 🏷️ دعم التصنيفات
- 🔘 أزرار تفاعلية
- 💾 قاعدة بيانات SQLite
- 🐳 دعم Docker

## المتطلبات

- Docker **أو**
- Python 3.8+

## النشر على Render.com (الطريقة الأسهل)

### الخطوة 1: رفع المشروع ل GitHub

المستودع جاهز ومُرفع بالفعل:
🔗 https://github.com/elias0878/telegram-image-bot

### الخطوة 2: إنشاء خدمة على Render

1. اذهب إلى [Render Dashboard](https://dashboard.render.com)
2. انقر **New +** ← **Web Service**
3. اختر **Connect GitHub**
4. اختر المستودع: `elias0878/telegram-image-bot`
5. في قسم **Settings**:
   - **Name**: `telegram-image-bot`
   - **Build Command**: (اتركه فارغاً - سيستخدم Dockerfile)
   - **Start Command**: `python server.py`
6. في قسم **Environment Variables**، أضف:

| المفتاح | القيمة |
|---------|--------|
| `BOT_TOKEN` | `7824666064:AAHbJ51UhVOrP_InSqF5CE2eMj2l1VqRSpQ` |
| `DATABASE_PATH` | `images.db` |
| `IMAGES_FOLDER` | `images` |
| `PORT` | `10000` |

7. اختر **Free Plan**
8. انقر **Create Web Service**
9. انتظر 2-3 دقائق للبناء

### الخطوة 3: بدء استخدام البوت

1. بعد اكتمال البناء، احصل على الرابط من لوحة التحكم
2. افتح تيليجرام وابحث عن البوت
3. أرسل `/start`

---

## التشغيل محلياً

### باستخدام Docker

```bash
# بناء الصورة
docker build -t telegram-bot .

# تشغيل الحاوية
docker run -d \
  --name telegram-bot \
  -e BOT_TOKEN=7824666064:AAHbJ51UhVOrP_InSqF5CE2eMj2l1VqRSpQ \
  -e DATABASE_PATH=images.db \
  -e IMAGES_FOLDER=images \
  -e PORT=10000 \
  -p 10000:10000 \
  telegram-bot
```

### بدون Docker

```bash
# تثبيت المتطلبات
pip install -r requirements.txt

# تشغيل البوت
python bot.py
```

---

## إضافة الصور

### الطريقة 1: عبر GitHub

```bash
# نسخ الصور لمجلد images/
cp /path/to/images/*.jpg images/

# إضافة للـ commit
git add images/
git commit -m "Add new images"
git push origin main
```

### الطريقة 2: عبر Python

```bash
# بعد وضع الصور في مجلد images/
python add_images.py add

# عرض الإحصائيات
python add_images.py stats
```

---

## أوامر البوت

| الأمر | الوصف |
|-------|-------|
| `/start` | بدء البوت |
| `/help` | عرض المساعدة |
| `/random` | صورة عشوائية |
| `/count` | عدد الصور |

---

## هيكل المشروع

```
telegram_bot/
├── Dockerfile        # ملف Docker
├── render.yaml       # ملف تكوين Render
├── bot.py           # الكود الرئيسي
├── server.py        # خادم الويب
├── add_images.py    # إدارة الصور
├── requirements.txt # المتطلبات
├── README.md        # هذا الملف
└── images/          # مجلد الصور
```

---

## استكشاف الأخطاء

### البوت لا يستجيب؟
- تحقق من Logs في Render Dashboard
- تأكد من صحة `BOT_TOKEN`

### لا توجد صور؟
- تأكد من إضافة الصور لمجلد `images/`
- شغل: `python add_images.py add`

### خطأ في الاتصال؟
- تحقق من أن المنفذ `10000` مفتوح
- تأكد من إعدادات المتغيرات البيئية

---

**البوت جاهز للنشر! 🎉**
