# بوت تيليجرام للصور العشوائية 📸

بوت تيليجرام بسيط يعرض صوراً عشوائية من قاعدة البيانات عند الضغط على زر.

## المميزات

- 🎲 عرض صور عشوائية من قاعدة البيانات
- 📊 تتبع عدد الصور
- 🏷️ دعم التصنيفات
- 🔘 أزرار تفاعلية
- 💾 قاعدة بيانات SQLite

## المتطلبات

- Python 3.8+
- python-telegram-bot 20.8

## التثبيت

```bash
# استنساخ المشروع
git clone https://github.com/yourusername/telegram-image-bot.git
cd telegram-image-bot

# تثبيت المتطلبات
pip install -r requirements.txt
```

## الإعداد

### 1. إنشاء بوت تيليجرام

1. ابحث عن @BotFather على تيليجرام
2. أرسل `/newbot` واتبع التعليمات
3. احصل على التوكن

### 2. إعداد المتغيرات البيئية

```bash
# نسخ ملف الإعدادات
cp .env.example .env

# تعديل الإعدادات
nano .env
```

### 3. إضافة الصور

```bash
# ضع صورك في مجلد images/
# ثم أضفها لقاعدة البيانات
python add_images.py add
```

## التشغيل

```bash
# تشغيل البوت
python bot.py
```

## النشر على Render.com

### الخطوة 1: رفع المشروع على GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/telegram-image-bot.git
git push -u origin main
```

### الخطوة 2: إنشاء خدمة على Render

1. اذهب إلى [Render Dashboard](https://dashboard.render.com)
2. انقر على **New +** واختر **Web Service**
3. ربط حساب GitHub
4. اختر المستودع
5. إعدادات البناء:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python server.py`

### الخطوة 3: إضافة المتغيرات البيئية

في إعدادات الخدمة على Render، أضف:
- `BOT_TOKEN`: توكن البوت
- `DATABASE_PATH`: `images.db`
- `IMAGES_FOLDER`: `images`

## أوامر البوت

| الأمر | الوصف |
|-------|-------|
| `/start` | بدء البوت |
| `/help` | عرض المساعدة |
| `/random` | الحصول على صورة عشوائية |
| `/categories` | عرض التصنيفات |
| `/count` | عدد الصور |

## إدارة الصور

```bash
# إضافة جميع الصور
python add_images.py add

# عرض الإحصائيات
python add_images.py stats

# قائمة الصور
python add_images.py list

# حذف صورة
python add_images.py delete [id]
```

## هيكل المشروع

```
telegram_bot/
├── bot.py           # الكود الرئيسي
├── server.py        # خادم الويب للنشر
├── add_images.py    # إدارة الصور
├── requirements.txt # المتطلبات
├── .env.example     # قالب الإعدادات
├── images/          # مجلد الصور
└── images.db        # قاعدة البيانات
```

## المساهمة

مرحباً بطلبات السحب والإصلاحات!

## الترخيص

MIT License
